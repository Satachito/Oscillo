# PiLyzer for PL2407AFE

購入したpicoLABO **PL2407AFE + Raspberry Pi Pico 2** 用のPiLyzer構成です。
ScoppyのAndroidアプリやファームウェアのコピーではありません。基板のAFEを利用し、PiLyzerのmacOS／Webアプリで測定します。

## 構成

- `board_config.h`: PL2407AFE専用GPIO・2ch・3レンジ換算値（board ID 3）。
  `../../firmware/pilyzer/board_config.h`がboard ID 3のときにこれを取り込みます。
- `build.sh`: 共通PiLyzerファームウェアをこの基板用にビルド。
- `tests/`: GPIO・レンジ・2ch取得の自動チェック。
- アプリは共通です。Webは[`../../Web`](../../Web)、macOSは`cd ../.. && ./Scripts/make-app.sh`。

アプリは接続したファームウェアから2ch・3レンジを取得します。既存Pico2用ファームウェアのビルド設定は変わりません。

## ビルドと書き込み

```sh
./Pico2/hardware/pl2407afe/build.sh
```

出力: `Pico2/hardware/pl2407afe/build/PiLyzer-PL2407AFE-Pico2.uf2`

Pico 2をBOOTSELで接続し、このUF2をコピーします。元のScoppyファームウェアは置き換わります。Scoppy Androidアプリへ戻す場合はScoppy用UF2を書き戻します。
このビルドはRP2350のPico 2用です。初代Pico/RP2040用ではありません。

## 配線・制御

| 機能 | GPIO |
|---|---|
| CH1 / CH2 | 26 / 27 |
| CH1レンジ制御 | 2, 3 |
| CH2レンジ制御 | 4, 5 |
| Logic D0–D7 | 6–13 |
| テスト出力（SG OUT） | 22（基板上1kΩ経由） |
| 信号生成器 | 16 サイン, 17 ホワイト, 18 ピンク, 19 ブラウン |

空きGPIOは 0, 1, 14, 15, 20, 21, 28 です。28だけがADC（ADC2）に繋がっています。
23/24/29はPico 2自身が使い、Pico 2 Wでは25も無線モジュール側に行きます。

テスト出力と信号生成器のピンは全ボード共通です。この基板のSG OUTが22にあり、
16–19が空いていたので、他のボードがそれに合わせました。PWMスライスも競合しません。
生成器の各ピンはPWMキャリアなので、電圧として使うにはRC（1kΩ + 10nF）が要ります。
**起動時に16–19はLow出力として駆動されます**ので、基板側がこれらのピンに何か
繋いでいないかメーカー回路図で確認してください。

CH3のAFEはありません。AC/DCは基板上スイッチで操作します。
各CHの制御ピンをA/Bとすると、±30Vは00、±6Vは01、±1.5Vは10です。
レンジ切替時には一旦00に戻し、両方のバイパスを同時に有効にしません。

## 入力保護

**BNCから見た絶対最大入力電圧は ±40 V です（メーカー公称）。レンジ設定によらず
一律です。**

分圧がレンジ切替スイッチより前にあるからです。BNCは直結で 940 kΩ + 120 kΩ を
見ており、レンジ別の直列抵抗（24230 / 4230 / 330 Ω）はその後ろ、減衰済みの信号を
受ける位置にあります。**アプリからレンジを変えても、入力端の耐圧は変わりません。**

ここから導いた数字（公称値からの計算で、実測ではありません）:

| | 値 |
|---|---:|
| BNCが最初に見る抵抗 | 940 kΩ |
| ±40 V を加えたときの入力電流 | 43 µA |
| 分圧比（120 kΩ が対GNDのシャントとして） | 0.113 |
| ±40 V のときの分圧点 | 4.53 V |

43 µA では抵抗は焼けません。±40 V を決めているのは電流ではなく、**分圧点が
能動素子に見せる電圧**の方です。5 V 動作のオペアンプの絶対最大入力
（V+ +0.3 V = 5.3 V）には 47 V 前後で到達する計算で、±40 V という公称値と
整合します。超えたときに起きるのは抵抗が焼けることではなく、オペアンプか
アナログスイッチの入力に食い込むことです。**壊れ方が静かで気づきにくい**ので、
上限は余裕を持って扱ってください。

120 kΩ がシャントなのか基準電圧側なのかは未確認です。どちらであってもBNCが
940 kΩ で受けることは変わりません。

### 使うときの線引き

- **絶対最大定格は常用の上限ではありません。**±30 V レンジに対する余裕は
  1.33 倍しかなく、リレーやモーターの逆起電力、電源投入時のリンギング、車載
  12 V 系のロードダンプ（40〜100 V に達します）を吸収できません。±30 V レンジは
  「30 V を測るため」ではなく「20 V くらいまでを安心して測るため」と考える方が
  安全です。
- **絶縁はありません。**GNDはUSB経由でホストの筐体に繋がっています。
- **商用電源は測れません。**100 V ACはピーク141 V、絶対最大の3.5倍です。
- ×10プローブを使えば入力は1/10になりますが、**プローブ自体の耐圧が足りている
  場合に限ります**。パネルのプローブ倍率も忘れずに合わせてください。

参考までに、自作の[PiLyzer AFE](../pilyzer-afe/README.md)は入力1 MΩで±25 Vレンジ、
連続100 V定格 ── 余裕が4倍あります。設計の狙いが違うので、そのつもりで。

## 電圧換算と検証状況

[メーカー回路図・説明](https://picolabo.org/pl2407afe/)のrev.1cを基準にしています。
届いた基板は**rev.1d**で、電源構成はrev.1cと同じでした。実測値は下の「校正」の項にあります。

公称抵抗値からのDC計算（アナログスイッチのオン抵抗・部品誤差は未補正）:

- 初段反転増幅器の利得は−0.1。
- 後段の基準電圧は `3.3 × 1 / 25 = 0.132 V`。
- レンジ別の直列抵抗Rsは24230 / 4230 / 330 Ω。
- `gain = 1.2 / (1 + Rs/1000 + Rs/10000)`。
- `offset = 13×0.132 − 12×(0.132/10000)/(1/Rs + 1/1000 + 1/10000)`。
- `Vinput = (Vadc − offset) / gain`。

ADC電源基準とAFE電源の差、抵抗誤差、スイッチ抵抗があるため、各CH・各レンジで0Vと既知電圧を確認し校正してください。最初はDC結合・±30VレンジでGND、続いて既知の小さい信号を確認します。
AFEの帯域はADCサンプリング速度とは別です。このファームウェアはADCをオーバークロックせず、1ch約495kSa/s、2ch約247kSa/sです。

上の式は公称抵抗値からの計算です。**実測で置き換えた値が
`board_config.h` に入っています**（下記）。

## 校正（2026-09-22）

乾電池4本の**5.01 V**を入力し、0 Vと合わせた2点で求めました。変換器側の読みは
4回測定して0.5 mV以内に揃っています。テスタが5.01 Vと5.02 Vを行き来したため、
**ゲインには約0.2 %の不確かさ**が残ります。律速はテスタで、こちらの読み取りの
寄与は0.03 %程度です。

| | | 公称 | 実測 |
| --- | --- | --- | --- |
| ±30 V | gain | 0.043395 | CH1 0.043974 / CH2 0.044140 |
| | offset | 1.577207 V | CH1 1.586325 V / CH2 1.582325 V |
| ±6 V | gain | 0.212277 | CH2 0.214483（CH1は測定不能） |
| | offset | 1.597473 V | CH2 1.602975 V |
| ±1.5 V | gain | 0.880411 | CH1 0.882035 / CH2 0.878957 |
| | offset | 1.677649 V | CH1 1.681950 V / CH2 1.684700 V |

レンジ表はチャンネルごとではなく**基板ごとに1本**なので、`board_config.h`には
**CH1とCH2の平均**を入れてあります。両チャンネルの差は0.5 %ほどで、これは
アプリ側のチャンネルごとのゲイン補正が吸収する範囲です。

±1.5 Vだけは5 Vがレンジ外なので、**1.3 Vの電池**で測りました。ただしテスタは
1.30 Vまでしか読めず、真値が1.295〜1.305のどこかだと**±0.38 %**の不確かさに
なります。そこで**校正済みの±6 Vレンジを物差しに使い**、それがこの電池を
1.3078 Vと読んだ値を採用しました。元の5.01 V測定の誤差だけが伝わるので
**±0.2 %**、約2倍の精度です。

この手が使えるのは回路が素直だからです。**±30 Vのゲインは5.01 Vと1.30 Vで
0.03 %一致**しており、直線性は確認済みです。

結果は公称値と**0.010 %差**でした。測定の不確かさ（±0.2 %）の20分の1なので、
区別がつきません。このレンジに限っては名目抵抗値からの計算が当たっていた、
ということです。

なお、これは「ADCの基準電圧が共通の原因では」という見立てを否定します。基準
電圧がずれていれば**全レンジが同じ率でずれる**はずで、±30 Vが+1.5 %、±6 Vが
+1.1 %、±1.5 Vが0 %という散り方にはなりません。レンジごとに効く抵抗の個体差、
と見る方が妥当です。offsetが一様に+5〜9 mV上振れしている件は別で、こちらは
後段の基準0.132 Vを作るR15/R26と、ADC基準の両方が候補として残ります。

## 実機立ち上げ（2026-09-22）

組み立てた基板は**rev.1d**（シルク表記）。Pico 2（シリアル
`9B5C456F3E8EAF87`）にファームウェア1.16、board ID 3で通電しています。

メーカーの公開回路図はrev.1cまでですが、電源構成は同じでした。基板端、取り付け
穴の脇に**GND / VBUS / +3V3 / −3V3**の4つのランドが並んでいます。U3〜U6はPicoの
真下に隠れるので、電源の確認はここで行います。オペアンプもアナログスイッチも
**±3.3 V**で動くので、−3V3が出ていなければ何も正しく動きません。

### 直した不具合

**VBUSがAFEに届いていなかった。** Picoの物理40番ピンのハンダ不良です。VBUSが
ないとU3もU4も動かないので、**+3V3と−3V3が同時に死にます**。このとき基板全体が
GPIOとADCの保護ダイオード経由で逆流した約2.2 Vで浮いており、+3V3ランドも2.23 V
という中途半端な値を示していました。レギュレータの出力なら3.3 Vか0 Vのどちらか
なので、**中途半端な電圧は「電源が来ていない」の兆候**です。オペアンプは
±3.3 Vを失って出力が正側レールに張り付き、ADCは2.22 V / 0.66 Vを読んでいました。

**GPIO2がU2の6番ピン（3S）に届いていなかった。** ソケットのハンダ不良です。CH1の
±1.5 Vレンジだけが効かない、という形で出ました。

### 残っている既知の不具合

**CH1の±6 Vレンジが切り替わりません。** U2のスイッチ4（4Z-4Y、13番-12番）が
閉じないためです。制御側は正常で、GPIO3はソケットからU2の14番（4S）まで導通が
あり、ヘッダ上でも3.27 V / 0 Vと正しく反転することを確認済みです。それでも
変換器電圧が0.1 mVも動きません。

U2内部のスイッチ4の故障か、12番・13番のハンダ浮きのどちらかですが、TSSOPの
隣接ピンを当たれないため切り分けていません。VBUSが来ていなかった期間、この
チップはVDDが2.2 V・VEEが0 Vのまま入力に信号が乗った状態で通電されており、
内部の保護ダイオードが順方向にバイアスされる条件が揃っていました。4回路のうち
1回路だけが死ぬ壊れ方は、それで説明がつきます。

**ファームもアプリも変更していません。**CH1で±6 Vを選ぶと、切り替わらずに
±30 Vのまま読み続けます。U2を載せ替えれば直る可能性があるので、レンジ表から
削るのではなく既知の不具合として残してあります。

### 入力開放での実測（未校正）

| レンジ | CH1 | CH2 | 公称offset |
| --- | --- | --- | --- |
| ±30 V | 1.5864 V | 1.5827 V | 1.5772 V |
| ±6 V | 1.5864 V ※効いていない | 1.6032 V | 1.5975 V |
| ±1.5 V | 1.6830 V | 1.6861 V | 1.6776 V |

CH2は3レンジとも公称値どおりの階段になっています。

### J1のピンソケット

**細い線では接触しません。**回路図上、J1の2・4・6番のGNDは同一ネットで、基板裏の
半田面では導通しています。それでも穴に差した線とは導通しないことがあり、ブレッド
ボード用の単線やデュポンのメス端子では2.54 mmソケットのバネ接点に届きません。
抵抗の足か、オスピンヘッダを差してそこに繋いでください。

## 自動チェック

`./Pico2/hardware/pl2407afe/tests/run.sh`でGPIO・各レンジのADC範囲・2ch取得とCH3拒否を検証します。共通ファームウェアの回帰テストは`./Pico2/firmware/pilyzer/tests/run.sh`です。
